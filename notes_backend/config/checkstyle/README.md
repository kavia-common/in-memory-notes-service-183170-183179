This minimal Checkstyle configuration is provided to satisfy CI linting that invokes `checkstyleMain`.
You can expand rules as needed. Currently, violations do not fail the build (ignoreFailures=true).
