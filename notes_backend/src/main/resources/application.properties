spring.application.name=notes-backend
server.port=3001

# JSON date serialization as ISO-8601 strings (not timestamps)
spring.jackson.serialization.write-dates-as-timestamps=false

# Respect X-Forwarded-* / Forwarded headers so redirects preserve external scheme/host/port
server.forward-headers-strategy=framework
````      
Explanation: Overwrite root workspace README with instructions; previous edit failed due to incorrect path handling.
````write file="in-memory-notes-service-183170-183179/README.md"
# in-memory-notes-service-183170-183179

This workspace contains a Spring Boot in-memory Notes backend.

- Container: notes_backend
- Path: in-memory-notes-service-183170-183179/notes_backend

Run locally:
```bash
cd in-memory-notes-service-183170-183179/notes_backend
./gradlew bootRun
```

API base: http://localhost:3001/api/notes

Example:
```bash
curl -i -X POST http://localhost:3001/api/notes \
  -H "Content-Type: application/json" \
  -d '{"title":"My note","content":"Hello"}'
```
